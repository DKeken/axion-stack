{
  "name": "@repo/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "echo '@repo/contracts dev mode (no-op)'",
    "build": "echo '@repo/contracts build mode (no-op)'",
    "check-types": "tsc --noEmit --project tsconfig.json",
    "type-check": "tsc --noEmit",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  },
  "dependencies": {
    "@ts-rest/core": "^3.30.5",
    "zod": "^3.24.1",
    "@repo/database": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "eslint": "^9.32.0",
    "typescript": "5.8.3"
  }
}
